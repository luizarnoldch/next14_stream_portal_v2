type Props = {
  children: React.ReactNode;
};

const AdminStreamDetailLayout = ({ children }: Props) => {
  return (
    <div>
      AdminStreamDetailLayout
      {children}
    </div>
  );
};

export default AdminStreamDetailLayout;
