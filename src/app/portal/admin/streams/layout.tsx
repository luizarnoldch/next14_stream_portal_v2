type Props = {
  children: React.ReactNode;
};

const AdminStreamsLayout = ({ children }: Props) => {
  return (
    <div>
      AdminStreamsLayout
      {children}
    </div>
  );
};

export default AdminStreamsLayout;
