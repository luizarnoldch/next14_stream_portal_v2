type Props = {
  children: React.ReactNode;
};

const AdminWorkersLayout = ({ children }: Props) => {
  return (
    <div>
      AdminWorkersLayout
      {children}
    </div>
  );
};

export default AdminWorkersLayout;
