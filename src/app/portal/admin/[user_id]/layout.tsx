type Props = {
  children: React.ReactNode;
};

const AdminProfileLayout = ({ children }: Props) => {
  return (
    <div>
      AdminProfileLayout
      {children}
    </div>
  );
};

export default AdminProfileLayout;
